# Competitors benchmarked against pygixml
# (ElementTree needs no entry -- it's in the Python standard library)
lxml
xmltodict
xmljson
yq                   # provides the `xq` command, benchmarked in bench_cli.py
untangle             # objectify-style XML->object mapper, sizes-only comparison in bench_sizes.py

# Benchmark tooling
pip-size            # github.com/MohammadRaziei/pip-size -- real install-size comparison
jinja2               # renders the standalone HTML report
tqdm                 # progress bars for the longer-running sweeps (throughput, scaling)
